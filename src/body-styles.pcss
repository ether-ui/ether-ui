body {
  font-family: "Public Sans", sans-serif;
  font-weight: 400;
  line-height: 160%;
  letter-spacing: 0.005em;
  color: var(--base-content);
}

body, html {
  min-height: 100%;
  background: var(--base-0);
}
