.btn {
  padding: 0.5rem 1rem;
  box-shadow: inset 0 0 0px 0px rgba(0, 0, 0, 0);
  cursor: pointer;
  line-height: 160%;
  font-size: 1rem;
  font-weight: 500;
  position: relative;
  background: var(--neutral);
  color: var(--neutral-content);
  transition: box-shadow 0.3s;
  border-radius: var(--rounded-radius);

  &.primary {
    background: var(--primary);
    color: var(--primary-content);
  }

  &.secondary {
    background: var(--secondary);
    color: var(--secondary-content);
  }

  &.tertiary {
    background: var(--tertiary);
    color: var(--tertiary-content);
  }

  &.primary-inverted {
    color: var(--primary);
    background: var(--primary-content);
    border: 1px solid var(--primary);
  }

  &.secondary-inverted {
    color: var(--secondary);
    background: var(--secondary-content);
    border: 1px solid var(--primary);
  }

  &.tertiary-inverted {
    color: var(--tertiary);
    background: var(--tertiary-content);
    border: 1px solid var(--primary);
  }

  &.small {
    padding: 0.25rem 0.5rem;
    font-size: 0.75rem;
  }
}

.btn:not([disabled]):hover {
  box-shadow: inset 0 0 50px 50px rgba(0, 0, 0, 0.1);
}

.btn:after {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  border: inset 0rem rgba(0, 0, 0, 0.1);
  transition: border 0.1s;
}

.btn.rounded-min:after {
  border-radius: 0.25rem;
}

.btn.rounded-max:after {
  border-radius: 1.5rem;
}

.btn:not([disabled]):active:after {
  border: inset 0.25rem rgba(0, 0, 0, 0.2);
}

.btn[disabled] {
  cursor: not-allowed;
  background: var(--gray-400) !important;
  color: var(--gray-800) !important;
}

.btn svg {
  width: 1.5rem;
  height: 1.5rem;
  margin: 0;
  padding: 0;
  display: inline-block;
  vertical-align: top;
}

.btn-border {
  border-style: solid;
  border-width: 1px;
}

a.btn {
  display: inline-block;
  text-align: center;
}
