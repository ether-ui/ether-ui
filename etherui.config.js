const breakpoints = require("./config/breakpoints");
const colors = require("./config/colors");
const themes = require("./config/themes");

/**
 * @typedef EtherUIConfig
 */
const config = {
  breakpoints: breakpoints,
  colors: colors,
  themes: themes,
};

module.exports = config;
